package rules

// common
const (
	EmptyMessage     = ""
	IsRequireMessage = "is required"
	IsEmptyMessage   = "is empty"
)

// string
const (
	IsInvalidTypeOfStringMessage = "is invalid type of string"
)

// boolean
const (
	IsInvalidTypeOfBooleanMessage = "is invalid type of boolean"
)
